services:
  - type: web
    name: dcn-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dcn_api_v2:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0